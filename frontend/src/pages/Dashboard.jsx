import{useEffect,useState}from"react";import{overview,frequencies,resolution}from"../api/api";import Dropdown from"../components/Dropdown";import Illustration from"../components/Illustration";
export default function Dashboard(){const[o,setO]=useState({}),[f,setF]=useState({}),[s,setS]=useState({}),[period,setPeriod]=useState("All time");useEffect(()=>{Promise.all([overview(),frequencies(),resolution()]).then(([a,b,c])=>{setO(a.data);setF(b.data);setS(c.data)}).catch(()=>{})},[]);const freq=f.category||{},fallback={"Water & sanitation":7,"Waste management":5,"Roads & transport":4,"Public lighting":2},data=Object.keys(freq).length?freq:fallback,max=Math.max(...Object.values(data).map(Number),1);const stats=[["Mean",s.mean,"Average resolution time"],["Median",s.median,"Middle observation"],["Mode",s.mode,"Most frequent value"],["Variance",s.variance,"Spread of observations"],["Std. deviation",s.standard_deviation,"Typical variation"],["Q1",s.q1,"25th percentile"],["Q3",s.q3,"75th percentile"],["IQR",s.iqr,"Middle 50% range"],["Upper fence",s.upper_fence,"Outlier threshold"]];return <><div className="page-intro row"><div><span className="eyebrow">BATCH 4 STATISTICS</span><h1>Analytics & insights</h1><p>Understand demand, urgency and how quickly civic cases are being resolved.</p></div><Dropdown label={period} items={["Today","This week","This month","All time"]} onSelect={setPeriod}/></div><div className="metric-grid compact">{[["Total",o.total||3], ["Open",o.open??2],["Resolved",o.resolved||0],["High / Critical",o.high_critical??2]].map(([t,v])=><div className="metric-card mini" key={t}><span>{t}</span><strong>{v}</strong><small>{period}</small></div>)}</div><div className="dashboard-grid"><div className="panel chart-panel"><div className="panel-title"><div><span className="eyebrow">DEMAND PROFILE</span><h3>Complaints by category</h3></div><span className="insight-chip">✦ AI insight</span></div>{Object.entries(data).map(([k,v])=><div className="bar-row" key={k}><span>{k}</span><div className="bar-track"><div className="bar" style={{width:`${Number(v)/max*100}%`}}/></div><b>{v}</b></div>)}<div className="service-visual"><Illustration type="city"/><div><b>Service demand snapshot</b><p>Use category frequency to identify where response capacity is most needed.</p></div></div></div><div className="panel stats-panel resolution-summary"><div className="panel-title"><div><span className="eyebrow">RESOLUTION TIME</span><h3>Statistical summary</h3></div><span className="summary-badge">9 measures</span></div><div className="summary-hero"><div className="summary-icon">◷</div><div><span>Typical resolution</span><strong>{s.median!=null?Number(s.median).toFixed(2):"—"}</strong><small>Median · time units</small></div></div><div className="stat-list">{stats.map(([k,v,desc])=><div className="stat-row" key={k}><div><b>{k}</b><small>{desc}</small></div><strong>{v==null?"—":Number(v).toFixed(2)}</strong></div>)}</div></div></div><div className="quote-banner compact-quote"><span className="quote-mark">“</span><div><p>Resolution-time statistics show not only how fast work gets done, but where process improvements may have the greatest effect.</p><small>Use statistics to support—not replace—human judgment.</small></div></div></>}
