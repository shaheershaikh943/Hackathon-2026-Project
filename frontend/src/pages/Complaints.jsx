import{useEffect,useState}from"react";import{complaints}from"../api/api";import Dropdown from"../components/Dropdown";import AISuggestion,{analyze}from"../components/AISuggestion";
const demo=[{id:101,description:"Continuous water leakage near the main road",location:"Block B",category:"Water",priority:"Urgent",status:"Open",department:"Water Services"},{id:102,description:"Garbage accumulation around community park",location:"Central Park",category:"Waste",priority:"High",status:"Open",department:"Sanitation"},{id:103,description:"Large pothole near the market entrance",location:"Market Street",category:"Roads",priority:"High",status:"In Review",department:"Road Maintenance"}];
export default function Complaints(){const[rows,setRows]=useState([]),[q,setQ]=useState(""),[priority,setPriority]=useState("All priorities"),[status,setStatus]=useState("All statuses"),[selected,setSelected]=useState(null);useEffect(()=>{complaints().then(r=>setRows(r.data)).catch(()=>setRows(demo))},[]);const all=rows.length?rows:demo;const filtered=all.filter(c=>(priority==="All priorities"||c.priority===priority)&&(status==="All statuses"||c.status===status)&&`${c.description} ${c.category} ${c.department} ${c.location}`.toLowerCase().includes(q.toLowerCase()));return <><div className="page-intro row"><div><span className="eyebrow">CASE MANAGEMENT</span><h1>Complaint desk</h1><p>Use filters to find the cases that need attention. Sample cases are included for demonstration.</p></div><a className="btn primary" href="/submit">＋ New complaint</a></div><div className="panel table-panel"><div className="table-toolbar"><div className="toolbar-left"><div className="search">⌕ <input placeholder="Search complaints..." value={q} onChange={e=>setQ(e.target.value)}/></div><Dropdown label={priority} items={["All priorities","Urgent","High","Medium","Low"]} onSelect={setPriority}/><Dropdown label={status} items={["All statuses","Open","In Review","Resolved"]} onSelect={setStatus}/></div><span>{filtered.length} records</span></div><div className="table-scroll"><table><thead><tr><th>ID</th><th>Complaint</th><th>Category</th><th>Priority</th><th>Status</th><th>Department</th><th></th></tr></thead><tbody>{filtered.map(c=><tr key={c.id}><td className="muted">#{String(c.id).padStart(4,"0")}</td><td><b>{c.description}</b><small>{c.location}</small></td><td><span className="tag">{c.category}</span></td><td><span className={'priority '+c.priority.toLowerCase()}>{c.priority}</span></td><td><span className="status-tag">{c.status}</span></td><td>{c.department}</td><td><button className="view-btn" onClick={()=>setSelected(c)}>View</button></td></tr>)}</tbody></table></div></div>{selected&&<div className="modal-backdrop" onClick={()=>setSelected(null)}><div className="modal" onClick={e=>e.stopPropagation()}><button className="modal-close" onClick={()=>setSelected(null)}>×</button><span className="eyebrow">CASE #{selected.id}</span><h2>{selected.description}</h2><p className="modal-location">⌖ {selected.location}</p><div className="modal-grid"><div><span>Priority</span><b>{selected.priority}</b></div><div><span>Status</span><b>{selected.status}</b></div><div><span>Department</span><b>{selected.department}</b></div></div><AISuggestion text={selected.description}/><button className="btn primary full" onClick={()=>setSelected(null)}>Close case view</button></div></div>}</>}
